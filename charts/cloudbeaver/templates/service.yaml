apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudbeaver.fullname" . }}-service
  labels:
    {{- include "cloudbeaver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8978
      protocol: TCP
      name: http
  selector:
    {{- include "cloudbeaver.selectorLabels" . | nindent 4 }}