apiVersion: v1
kind: Service
metadata:
  name: {{ include "convex.backend.fullname" . }}
  labels:
    {{- include "convex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 3210
      protocol: TCP
      name: http
  selector:
    {{- include "convex.backend.selectorLabels" . | nindent 4 }}