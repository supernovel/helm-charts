apiVersion: v1
kind: Service
metadata:
  name: {{ include "convex.dashboard.fullname" . }}
  labels:
    {{- include "convex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: 6791
      protocol: TCP
      name: http
  selector:
    {{- include "convex.dashboard.selectorLabels" . | nindent 4 }}