apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "qemus-qemu.fullname" . }}-storage
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qemus-qemu.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- with .Values.vm.storageClassName }}
  storageClassName: {{ . }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.vm.diskSize }}
